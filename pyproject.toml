[project]
name = "kalillac-api-runtime"
version = "0.1.0"
description = "Portable FastAPI runtime for the Kalillac API"
requires-python = ">=3.13"
dependencies = [
    "fastapi>=0.141.1",
    "httpx>=0.28.1",
    "pytest>=9.1.1",
    "uvicorn>=0.53.0",
]
